{
  "name": "konome-live",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "箱庭観測録 -此芽の観測室- / Arca Hortus 疑似ライブ配信ページ",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "dependencies": {
    "pixi-live2d-display": "^0.5.0-beta",
    "pixi.js": "^7.4.3"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.11"
  }
}
